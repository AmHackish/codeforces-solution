#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
	
	int n;
	cin >> n;
	
	while(n--)
	{
		string s,t;
		cin >> s >> t;

		vector<int> a(26);
		vector<int> b(26);
		for(int i=0;i<s.length();i++)
			a[s[i]-'a']++;

		
	}
	return 0;
}  